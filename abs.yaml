platform:
  linux-64:
    enabled: true
  linux-aarch64:
    enabled: true
  osx-arm64:
    enabled: true

build:
  error_overlinking: true

# Upload to staging so pypdfium2 can build before this PR merges
staging_channel_upload_target: pdfium-7776-staging
